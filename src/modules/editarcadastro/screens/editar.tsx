/* eslint-disable prettier/prettier */// eslint-disable-next-line prettier/prettier
import React from "react";
import { ScrollView, TextInput } from "react-native";

const Editar = () => {

return (
    <ScrollView>
        <TextInput></TextInput>
    </ScrollView>
);
};
export default Editar;
